/*
#include <iostream>
#include "myreadwrite.h"

int whatever() {
    ofstream ofile("numbers.dat");
    double d=13.3;
    myWrite(ofile,d);
    int x=5;
    myWrite(ofile,x);

    ifstream ifile("numbers.dat");
    double readd;
    int readx;

    myRead(ifile,readd);
    myRead(ifile,readx);

    return 0;
}
*/